{"id":487560539,"lang":"cpp","time":"11 months, 3 weeks","timestamp":1619898627,"status_display":"Accepted","runtime":"960 ms","url":"/submissions/detail/487560539/","is_pending":"Not Pending","title":"Escape a Large Maze","memory":"83.3 MB","code":"\nclass Solution {\npublic:\n    int maxx = 1e6;\n    set<array<int, 2>> mp;    \n    int sz;\n    array<int, 4> dx{0, 0, 1, -1};\n    array<int, 4> dy{1, -1, 0, 0};\n    array<int, 2> src;\n    array<int, 2> dst;\n    bool reachable = false;\n    bool bfs(array<int, 2> p) {\n        set<array<int, 2>> visited;\n        queue<array<int, 2>> q;\n        q.push(p);\n        while(!q.empty() && visited.size() < sz) {\n            auto t = q.front(); q.pop();\n            if (t[0] < 0 || t[0] >= maxx || t[1] < 0 || t[1] >= maxx || mp.count(t) || visited.count(t)) continue;\n            visited.insert(t);\n            if (p[0] == src[0] && p[1] == src[1] && t[0] == dst[0] && t[1] == dst[1]) {\n                reachable = true;\n                return true;\n            }\n            for (int i = 0; i < 4; ++i) {\n                q.push({t[0] + dx[i], t[1] + dy[i]});\n            }\n        }\n        return visited.size() >= sz;\n    }\n    bool isEscapePossible(vector<vector<int>>& blocked, vector<int>& source, vector<int>& target) {\n        sz = blocked.size() * blocked.size() - blocked.size();\n        for (const auto& p : blocked) {\n            mp.insert({p[0], p[1]});\n        }\n        src = {source[0], source[1]};\n        dst = {target[0], target[1]};\n        auto rs = bfs(src);\n        if (reachable) return true;\n        auto rd = bfs(dst);\n        return rs && rd;\n        \n        \n    }\n};","compare_result":"111111111111111111111111111111","title_slug":"escape-a-large-maze"}