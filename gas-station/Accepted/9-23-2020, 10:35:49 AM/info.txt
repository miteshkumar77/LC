{"id":399691724,"lang":"cpp","time":"1 year, 6 months","timestamp":1600871749,"status_display":"Accepted","runtime":"12 ms","url":"/submissions/detail/399691724/","is_pending":"Not Pending","title":"Gas Station","memory":"10.3 MB","code":"class Solution {\npublic:\n    int canCompleteCircuit(vector<int>& gas, vector<int>& cost) {\n        \n        int n = gas.size(); \n        vector<int> pre(n); \n        pre[0] = gas[0] - cost[0]; \n        int sum = pre[0]; \n        for (int i = 1; i < n; ++i) {\n            sum += gas[i] - cost[i]; \n            pre[i] = min(sum, pre[i - 1]); \n        }\n        \n        vector<int> suffmin(n); \n        sum = 0;\n        int prevmin = 0;\n        for (int i = n - 1; i >= 1; --i) {\n            \n            int dif = gas[i] - cost[i]; \n            sum += dif;\n            if (dif + prevmin >= 0 && sum + pre[i - 1] >= 0) {\n                return i;\n            }\n            prevmin = min(prevmin + dif, sum);\n\n        }\n        int dif = gas[0] - cost[0]; \n        sum += dif;\n        prevmin = min(prevmin + dif, sum); \n\n        if (prevmin >= 0) {\n            return 0; \n        }\n        \n        \n        return -1;\n        \n    }\n};","compare_result":"1111111111111111111111111111111","title_slug":"gas-station"}